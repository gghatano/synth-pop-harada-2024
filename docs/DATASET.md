# DATASET — データの取り扱いポリシー

本ドキュメントは、`synthpop-jp` に同梱される（または同梱しない）データの出所・ライセンス・再配布可否をまとめたものです。
研究用途で公開集計表を扱う OSS として、**何を配布し、何を配布しないか** を最初に明確にします。

本プロジェクトの開発規約全般は `CLAUDE.md` を参照してください。

---

## 1. 同梱されるデータ

### 1.1 `data/sample_case/` — 完全合成ダミー

- **内容**: `synthpop-jp quickstart` および単体テストで利用する入力サンプル（`family_type_counts.csv`、`age_distribution.csv` などの集計表）。
- **出所**: 実在する統計に由来しません。`scripts/generate_sample_case.py`（Phase 1 で実装予定）が固定 seed で生成する **完全合成ダミー** です。
- **ライセンス**: 本リポジトリと同じ Apache-2.0。
- **利用目的**: ソフトウェアの動作確認・テスト・デモのみ。研究知見の根拠として引用してはいけません。
- **注意**: この合成データから実在する個人・世帯を同定することはできません（乱数から合成された数値です）。

### 1.2 `data/templates/estat/`（Phase 1 以降）

- **内容**: e-Stat API レスポンスを本パッケージの入力 CSV 形式に変換するための **スキーマ定義・列マッピング** のみ。
- **出所**: e-Stat（政府統計の総合窓口）が公開する統計表スキーマを参照した構造定義ファイル。
- **ライセンス**: 構造定義ファイル自体は本リポジトリと同じ Apache-2.0。
- **再配布しない**: e-Stat から取得した **実データ本体** はこのディレクトリに置きません。

---

## 2. 同梱されないデータ

### 2.1 e-Stat 実データ（政府統計）

- **理由**: e-Stat の利用規約および統計法 §44 に基づき、**再配布は行いません**。
- **取得方法**: ユーザー自身が `scripts/fetch_estat.py`（Phase 2 以降で提供予定）を使って e-Stat API から取得します。取得したデータはユーザーのローカル環境に保存され、本リポジトリの管理外となります。
- **出典表記義務**: e-Stat 由来のデータを使った結果物（論文・レポート・生成された合成人口を含む）を公開する場合、e-Stat の利用規約に従って **出典を明記する義務** があります。`synthpop-jp report` コマンドが生成する `report.md` には、使用した e-Stat 統計表 ID と出典文字列が自動で埋め込まれます（Phase 4 で実装）。

### 2.2 評価用の実個票（microdata）

- DCR / NNDR / CAP など秘匿性評価の一部は、本来「実個票」との距離や属性推論成功率を測るものです。
- 実個票はプライバシー保護の観点から本リポジトリには同梱しません。評価者は統計法 §33 に基づく申請等で個別に入手し、各自のローカル環境で評価してください。
- 実個票が入手できない場合の代替プロトコル（semi-synthetic 設定）は `docs/assumptions.md`（Phase 0 で整備）を参照してください。

---

## 3. 関連法令・規約のサマリ

| 対象 | 根拠 | 要旨 |
|---|---|---|
| e-Stat の統計表 | e-Stat 利用規約 | 出典表記義務・加工した場合は加工内容の明示・再配布条件の確認 |
| 匿名データ・調査票情報 | 統計法 §44 / §33 | 目的外使用禁止、二次利用時の申請義務 |
| 合成人口データの公開 | 本リポジトリのライセンス (Apache-2.0) | 秘匿性評価を通過した合成データに限り再配布可能。ただし元データの規約が優先する |

**重要**: 本 OSS は「公開集計表だけから合成人口を作る」ことを想定していますが、合成の過程で実個票を使った評価（例: CAP）を挟む場合、その評価データ自体の規約を **必ず** 確認してください。

---

## 4. 合成データ自体のライセンスと利用範囲

- 本リポジトリのコードで生成した合成人口データは、**生成に使った入力データのライセンス** と **本リポジトリの Apache-2.0** の両方に従います。
- 例: 入力が e-Stat の集計表の場合、生成した合成人口を二次配布するには e-Stat の再配布条件（出典表記など）を満たす必要があります。
- 研究用途に限定する運用を **推奨** します。商用利用の可否は、入力データの規約を個別に確認してください。

---

## 5. 新しいデータを追加するときのチェックリスト

新しい集計表テンプレートやサンプルデータを本リポジトリに追加する際は、PR に以下を満たしてください。

- [ ] 出所（URL または DOI）が明示されている
- [ ] ライセンス（Apache-2.0 互換か否か）が確認されている
- [ ] 実データ本体ではなくスキーマ定義のみを同梱している（実データは `scripts/` からユーザー取得）
- [ ] 個人情報・匿名化前データが含まれていない
- [ ] 本 DATASET.md の §1 または §2 に追記している
- [ ] `report.md` 自動埋込の出典文字列を `evaluate/report.py` に登録している（Phase 4 以降）

---

## 6. 問い合わせ

データ取扱いに関する懸念や相談は、GitHub Issue（ラベル `dataset`）または `adad.0405@gmail.com` まで。
