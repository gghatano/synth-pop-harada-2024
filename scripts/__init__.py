"""Repository-internal scripts (not part of the synthpop_jp package).

These modules can be imported as ``scripts.<name>`` from tests. They are
typically invoked as ``uv run python scripts/<name>.py`` rather than via
the package import path.
"""
