"""Domain layer: pydantic models, protocols, and shared types.

The ``domain`` package defines the I/O boundary of synthpop-jp. Internal
SA state (``PopulationArrays``) lives in :mod:`synthpop_jp.optimize.state`;
the domain models are the interchange format with external callers.
"""
